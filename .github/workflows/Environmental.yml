name: "Environmental"
on:
    push:
        branches:
            - dev
jobs:
    build:
        environment:
            name: stage
            
        runs-on: ubuntu-latest
        steps:
            - name: Chekout Repo
              uses: actions/checkout@v4

